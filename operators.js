"use strict";
// Casi Habilitado.
let edad = 42;
let esMayor = edad >= 18;
let habilitado = esMayor ? "Si" : "No";
console.log("Habilitador para el curso: " + habilitado);

// Caso Inhabilitado.
edad = 17;
esMayor = edad >= 18;
habilitado = esMayor ? "Si" : "No";
console.log("Habilitador para el curso: " + habilitado);
